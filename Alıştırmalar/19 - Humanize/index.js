/**
 * @author İsa Toltar
 * 
 * Problem: Write a JavaScript function to get humanized number with the correct 
 * suffix such as 1st, 2nd, 3rd or 4th. 
 * 
 * 
 * Test Data :
 * console.log(humanize(1));
 * console.log(humanize(20));
 * console.log(humanize(302));
 * "1st"
 * "20th"
 * "302nd"
 */